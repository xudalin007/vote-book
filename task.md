# 图书投票应用任务管理计划

## 1. 项目目标概述

本项目要实现一个网页版本的图书投票应用。MVP 阶段支持展示约 6 本图书，用户可点击按钮为图书投票，票数由 Node.js 后端处理并存储到本地 JSON 文件中。

项目当前目标是在已完成投票 MVP、认证、投票限制和后台基础能力的基础上，规划并准备实现后台维护完整图书信息能力。

## 2. MVP、认证与后台扩展开发范围

### 2.1 投票 MVP 已包含范围

- 展示图书列表。
- 展示每本书的基础信息和当前票数。
- 支持用户点击按钮投票。
- 投票成功后更新对应图书票数。
- 后端提供图书列表接口和投票接口。
- 使用 `book.json` 存储图书信息。
- 使用 `vote.json` 存储投票信息。
- 提供基础错误处理和用户提示。
- 使用文件读写方案保证 MVP 阶段的数据可持久化。

### 2.2 基础认证扩展包含范围

- 新增 `user.json` 保存用户信息。
- 支持用户注册。
- 支持用户登录。
- 支持用户退出登录。
- 支持获取当前登录用户信息。
- 前端展示未登录状态、注册/登录入口、已登录用户名和退出入口。
- 密码使用 Node.js 内置 `crypto` 进行哈希和盐处理，不明文保存。
- 登录态使用 Cookie + 服务端内存 Session。
- 未登录用户仍可浏览图书列表，但不能投票。
- 所有已登录用户，包括管理员，都可以自行修改自己的登录密码。

### 2.3 投票限制扩展包含范围

- 只允许已登录用户投票。
- 未登录用户点击投票时提示先登录。
- 每个用户最多累计投 3 票。
- 3 票可以投给同一本书，也可以投给不同图书。
- 用户投满 3 票后，后端拒绝继续投票并返回明确错误。
- 前端展示当前用户已用票数或剩余票数。
- `vote.json` 需要保存每个用户的投票记录，用于判断投票次数。

### 2.4 管理员后台扩展包含范围

- 支持管理员角色。
- 管理员可以访问后台页面。
- 管理员可以查看图书列表。
- 管理员可以修改已有图书的书名、作者、简介和封面地址。
- 管理员可以新增图书，从而增加候选图书本数。
- 管理员新增图书时必须填写书名、作者、简介和封面地址。
- 管理员可以删除图书，从而减少候选图书本数。
- 管理员新增或删除图书时，需要同步维护 `book.json` 和 `vote.json`。
- 普通用户不能访问后台页面和后台接口。
- 管理员可以发起新一轮投票，清空票数和用户投票记录。

### 2.5 不包含范围

- 反作弊机制。
- 找回密码、重置密码、邮箱验证、手机号验证。
- 第三方登录。
- Session 持久化；服务重启后允许登录态失效。
- 数据库接入。
- 图书分类、排序、标签等高级字段维护。
- 独立排行榜页面。
- 评论、收藏、分享等社交功能。

## 3. 任务状态说明

任务状态统一使用以下字段：

- `todo`：尚未开始。
- `in-progress`：正在进行。
- `done`：已完成并通过验收。

## 4. 阶段任务清单

### 4.1 阶段一：项目初始化

| 任务 | 状态 | 验收标准 |
| --- | --- | --- |
| 确认项目目录结构 | done | 项目根目录结构清晰，前端、后端、数据文件位置明确。 |
| 初始化 Node.js 项目 | done | 存在可用的 `package.json`，能通过脚本启动服务。 |
| 确定技术栈和运行方式 | done | 明确使用的 Node.js Web 框架或原生方案，并记录启动命令。 |
| 配置基础静态资源服务 | done | 浏览器可以访问前端页面资源。 |

### 4.2 阶段二：数据文件设计

| 任务 | 状态 | 验收标准 |
| --- | --- | --- |
| 创建 `book.json` | done | 文件包含约 6 本图书，每本书有 `id`、`title`、`author`、`description`、`coverUrl` 字段。 |
| 创建 `vote.json` | done | 文件以图书 `id` 为 key，票数为 value，初始数据合法。 |
| 实现数据读取规则 | done | 后端能够读取 `book.json` 和 `vote.json`，并处理文件缺失或 JSON 格式错误。 |
| 实现票数默认值规则 | done | 当某本书在 `vote.json` 中不存在时，接口返回票数 `0`。 |

### 4.3 阶段三：后端接口开发

| 任务 | 状态 | 验收标准 |
| --- | --- | --- |
| 实现 `GET /api/books` | done | 接口返回图书基础信息和对应票数，响应结构符合 `spec.md`。 |
| 实现 `POST /api/votes` | done | 接口接收 `bookId`，校验图书存在后为对应图书票数加 1。 |
| 实现统一成功响应 | done | 成功响应包含 `success: true` 和 `data` 字段。 |
| 实现统一错误响应 | done | 错误响应包含 `success: false`、`error.code` 和 `error.message` 字段。 |
| 实现文件写入流程 | done | 投票成功后，新票数能正确写回 `vote.json`。 |
| 实现基础并发保护 | done | 同一时间写入 `vote.json` 时不会出现明显数据覆盖风险。 |

### 4.4 阶段四：前端页面开发

| 任务 | 状态 | 验收标准 |
| --- | --- | --- |
| 实现页面基础布局 | done | 页面包含标题区、说明区、图书列表区和状态提示区。 |
| 实现图书卡片组件 | done | 每张卡片展示封面、书名、作者、简介、票数和投票按钮。 |
| 实现图书列表加载 | done | 页面初始化时调用 `GET /api/books` 并正确渲染数据。 |
| 实现加载状态展示 | done | 数据请求期间页面有明确加载提示。 |
| 实现错误状态展示 | done | 图书加载失败时页面展示可理解的错误提示。 |

### 4.5 阶段五：投票交互联调

| 任务 | 状态 | 验收标准 |
| --- | --- | --- |
| 绑定投票按钮事件 | done | 点击某本书按钮后能向后端提交对应 `bookId`。 |
| 实现投票中状态 | done | 投票请求进行中，按钮短暂禁用，避免连续快速点击。 |
| 实现投票成功更新 | done | 后端返回成功后，页面对应图书票数立即更新。 |
| 实现投票失败恢复 | done | 投票失败后按钮恢复可点击，并展示错误提示。 |
| 完成前后端接口联调 | done | 从页面点击投票到 `vote.json` 更新的完整链路可用。 |

### 4.6 阶段六：错误处理与基础稳定性

| 任务 | 状态 | 验收标准 |
| --- | --- | --- |
| 处理图书不存在错误 | done | 传入不存在的 `bookId` 时，后端返回明确错误。 |
| 处理数据文件读取失败 | done | 文件读取异常时服务不崩溃，并返回统一错误响应。 |
| 处理数据文件写入失败 | done | 写入失败时不返回投票成功，前端展示失败提示。 |
| 验证 JSON 数据格式 | done | 非法 JSON 能被识别，并返回可定位问题的错误信息。 |
| 检查基础日志输出 | done | 后端关键错误有日志记录，便于排查问题。 |

### 4.7 阶段七：验收与后续规划

| 任务 | 状态 | 验收标准 |
| --- | --- | --- |
| 完成 MVP 功能验收 | done | 图书展示、投票、票数更新、文件持久化均可正常工作。 |
| 执行基础接口验证 | done | `GET /api/books` 和 `POST /api/votes` 返回结果符合预期。 |
| 执行基础页面验证 | done | 浏览器中可以完成完整投票流程。 |
| 更新项目进度摘要 | done | `task.md` 中当前进度与实际开发状态一致。 |
| 记录后续迭代事项 | done | 排行榜、数据库迁移等扩展项已记录。 |

### 4.8 阶段八：基础用户认证规划与开发

| 任务 | 状态 | 验收标准 |
| --- | --- | --- |
| 更新认证模块规格 | done | `spec.md` 已说明注册、登录、退出登录、当前用户接口、数据结构和认证流程。 |
| 更新认证模块任务计划 | done | `task.md` 已新增基础认证阶段任务、范围和验收标准。 |
| 创建 `user.json` | done | 文件存在且结构为用户数组，用户字段包含 `id`、`username`、`passwordHash`、`passwordSalt`、`createdAt`。 |
| 实现用户数据读写规则 | done | 后端能读取和写入 `user.json`，并处理文件缺失、非法 JSON、重复用户名等情况。 |
| 实现密码哈希工具 | done | 注册时密码使用随机盐和哈希保存，`user.json` 中不出现明文密码。 |
| 实现 Session 管理 | done | 后端能创建、读取、销毁内存 Session，并通过 Cookie 保存会话标识。 |
| 实现 `POST /api/auth/register` | done | 用户可注册新账号，重复用户名返回明确错误。 |
| 实现 `POST /api/auth/login` | done | 已注册用户可登录，密码错误返回明确错误。 |
| 实现 `POST /api/auth/logout` | done | 已登录用户可退出登录，后端清除 Session 和 Cookie。 |
| 实现 `GET /api/auth/me` | done | 前端可获取当前认证状态和登录用户基础信息。 |
| 增加前端认证入口 | done | 页面提供注册、登录、退出登录入口，并显示当前登录用户名。 |
| 实现认证前端状态处理 | done | 注册、登录、退出登录过程中有加载、成功和失败提示。 |
| 完成认证接口验证 | done | 注册、登录、获取当前用户、退出登录接口均返回符合 `spec.md` 的结构。 |
| 完成认证页面验证 | done | 浏览器中可以完成注册、登录、显示用户名、退出登录完整流程。 |
| 同步认证开发进度 | done | 完成开发和验证后更新本文件中的任务状态、进度摘要和验证记录。 |

### 4.8.1 阶段八补充：密码可见切换

| 任务 | 状态 | 验收标准 |
| --- | --- | --- |
| 更新密码可见规格 | done | `spec.md` 已明确所有密码输入框需要显示/隐藏密码按钮。 |
| 登录密码可见切换 | done | 登录密码输入框旁有切换按钮，点击后可在明文和隐藏状态间切换。 |
| 注册密码可见切换 | done | 注册密码输入框旁有切换按钮，点击后可在明文和隐藏状态间切换。 |
| 确认密码可见切换 | done | 确认密码输入框旁有切换按钮，点击后可在明文和隐藏状态间切换。 |
| 验证密码存储不变 | done | 切换显示密码只影响前端展示，`user.json` 仍不保存明文密码。 |
| 同步密码切换进度 | done | 完成开发和验证后更新任务状态、进度摘要和验证记录。 |

### 4.9 阶段九：登录投票与三票限制

| 任务 | 状态 | 验收标准 |
| --- | --- | --- |
| 更新投票权限规格 | done | `spec.md` 已明确只允许登录用户投票。 |
| 更新三票限制规格 | done | `spec.md` 已明确每个用户最多累计投 3 票，且可投给同一本书或不同图书。 |
| 设计新版 `vote.json` 结构 | done | `spec.md` 已定义 `bookVotes` 和 `userVotes`，支持统计用户投票次数。 |
| 迁移旧版 `vote.json` 结构 | done | 后端启动时能将旧版 `{ "book-001": 0 }` 结构迁移为新版结构。 |
| 修改投票接口登录校验 | done | 未登录调用 `POST /api/votes` 返回 `UNAUTHORIZED`，不会写入票数。 |
| 实现用户投票次数校验 | done | 用户已投 3 票时继续投票返回 `VOTE_LIMIT_REACHED`。 |
| 写入用户投票记录 | done | 每次投票成功后，`vote.json.userVotes[userId]` 追加一条投票记录。 |
| 返回剩余投票次数 | done | 投票接口和当前用户接口返回已用票数与剩余票数。 |
| 更新前端投票状态 | done | 未登录用户提示先登录，登录用户看到剩余票数，投满后投票入口不可继续提交。 |
| 验证登录投票限制 | done | 验证未登录不能投票、登录后最多 3 票、第 4 票被拒绝。 |

### 4.10 阶段十：管理员后台图书管理

| 任务 | 状态 | 验收标准 |
| --- | --- | --- |
| 更新管理员后台规格 | done | `spec.md` 已说明管理员可修改书名、作者、简介、封面，增加图书、删除图书。 |
| 扩展用户角色字段 | done | `user.json` 用户记录支持 `role` 字段，普通用户为 `user`，管理员为 `admin`。 |
| 提供管理员账号初始化方式 | done | 项目有明确方式创建或初始化管理员账号。 |
| 实现管理员权限校验 | done | 后台接口要求登录且角色为 `admin`，普通用户返回 `FORBIDDEN`。 |
| 实现 `GET /api/admin/books` | done | 管理员可获取后台图书列表和票数。 |
| 实现 `POST /api/admin/books` | done | 管理员可新增图书，并同步初始化该书票数。 |
| 实现 `PATCH /api/admin/books/:bookId` | done | 管理员可修改指定图书书名。 |
| 实现 `DELETE /api/admin/books/:bookId` | done | 管理员可删除指定图书，并同步清理票数和相关投票记录。 |
| 增加后台管理页面 | done | 管理员登录后可进入后台页面维护图书。 |
| 增加前端后台入口 | done | 只有管理员登录后显示后台入口，普通用户不可见。 |
| 完成后台接口验证 | done | 验证管理员可增删改查，普通用户和未登录用户被拒绝。 |
| 完成后台页面验证 | done | 浏览器中可以完成修改书名、新增图书、删除图书流程。 |
| 同步后台开发进度 | done | 完成开发和验证后更新任务状态、进度摘要和验证记录。 |

### 4.10.1 阶段十补充：后台维护完整图书信息

| 任务 | 状态 | 验收标准 |
| --- | --- | --- |
| 更新完整图书信息规格 | done | `spec.md` 已明确后台新增和编辑图书时必须包含书名、作者、简介和封面地址。 |
| 更新新增图书接口设计 | done | `spec.md` 已定义 `POST /api/admin/books` 请求体包含 `title`、`author`、`description`、`coverUrl`。 |
| 更新编辑图书接口设计 | done | `spec.md` 已定义 `PATCH /api/admin/books/:bookId` 可修改 `title`、`author`、`description`、`coverUrl`。 |
| 扩展新增图书后端校验 | todo | 新增图书时缺少书名、作者、简介或封面地址应返回明确错误。 |
| 扩展编辑图书后端校验 | todo | 编辑图书时缺少书名、作者、简介或封面地址应返回明确错误。 |
| 扩展后台新增表单 | todo | 后台新增图书表单包含书名、作者、简介、封面地址输入项。 |
| 扩展后台编辑表单 | todo | 后台每本图书都可编辑书名、作者、简介、封面地址。 |
| 确认前台完整展示 | todo | 投票页面每本图书展示封面、书名、作者和简介，包括新增图书。 |
| 完成完整图书信息验证 | todo | 验证新增图书和编辑图书后，前台和后台均显示完整信息。 |
| 同步完整图书信息进度 | todo | 完成开发和验证后更新任务状态、进度摘要和验证记录。 |

### 4.11 阶段十一：用户自行修改密码

| 任务 | 状态 | 验收标准 |
| --- | --- | --- |
| 更新修改密码规格 | done | `spec.md` 已明确所有已登录用户可以修改自己的登录密码。 |
| 设计修改密码接口 | done | `spec.md` 已定义 `POST /api/auth/change-password` 请求、响应和错误场景。 |
| 实现修改密码接口 | done | 已登录用户可通过当前密码和新密码修改自己的密码。 |
| 校验当前密码 | done | 当前密码错误时返回 `INVALID_CURRENT_PASSWORD`，不更新 `user.json`。 |
| 更新密码哈希和盐 | done | 修改成功后仅更新当前用户 `passwordHash` 和 `passwordSalt`，不保存明文密码。 |
| 增加修改密码前端入口 | done | 登录后页面展示“修改密码”入口，普通用户和管理员都可见。 |
| 增加修改密码表单 | done | 表单包含当前密码、新密码、确认新密码，并支持显示/隐藏密码。 |
| 完成修改密码验证 | done | 验证错误当前密码失败、正确当前密码成功、新密码可用于重新登录。 |
| 同步修改密码进度 | done | 完成开发和验证后更新任务状态、进度摘要和验证记录。 |

### 4.12 阶段十二：管理员发起新一轮投票

| 任务 | 状态 | 验收标准 |
| --- | --- | --- |
| 更新新一轮投票规格 | done | `spec.md` 已明确管理员可发起新一轮投票。 |
| 设计新一轮投票接口 | done | `spec.md` 已定义 `POST /api/admin/vote-rounds` 响应和处理规则。 |
| 实现新一轮投票接口 | done | 管理员调用接口后，所有图书票数归零，用户投票记录清空。 |
| 实现新一轮投票权限校验 | done | 未登录返回 `UNAUTHORIZED`，普通用户返回 `FORBIDDEN`。 |
| 增加后台新一轮入口 | done | 管理员后台页面展示“发起新一轮投票”按钮。 |
| 增加高风险二次确认 | done | 发起前必须确认会清空票数和用户投票记录。 |
| 刷新前后台状态 | done | 发起成功后刷新图书列表、后台列表和当前用户剩余票数。 |
| 完成新一轮投票验证 | done | 验证票数归零、用户恢复 3 票、图书和用户账号不被删除。 |
| 同步新一轮投票进度 | done | 完成开发和验证后更新任务状态、进度摘要和验证记录。 |

## 5. 当前项目进度摘要

截至当前文档更新时：

- `spec.md` 已更新，新增登录投票、三票限制和管理员后台管理规格。
- `task.md` 已更新，新增登录投票限制和管理员后台任务拆分。
- Node.js 后端服务已完成。
- 数据文件 `book.json`、`vote.json` 和 `user.json` 已创建。
- 前端页面、投票交互、认证入口和静态资源已完成。
- MVP 核心链路已通过基础验证。
- 认证模块已完成基础注册、登录、退出登录、当前用户状态检查。
- 认证模块已完成接口验证；测试用户已清理，`user.json` 保留现有用户数据。
- 密码可见切换已完成开发和基础验证。
- 登录后投票限制、每用户 3 票限制、管理员后台图书管理已完成开发和基础验证。
- `vote.json` 已迁移为 `bookVotes` 和 `userVotes` 结构，并保留原始图书票数。
- 用户自行修改密码已完成开发和基础验证。
- 管理员发起新一轮投票已完成开发和基础验证。
- 后台维护完整图书信息已完成规格和任务规划，业务代码尚未修改。

当前整体进度：

- 项目规划：`done`
- 任务拆分：`done`
- 项目初始化：`done`
- 数据文件设计：`done`
- 后端接口开发：`done`
- 前端页面开发：`done`
- 投票交互联调：`done`
- 稳定性处理：`done`
- MVP 验收：`done`
- 基础认证规格更新：`done`
- 基础认证任务计划：`done`
- 基础认证代码开发：`done`
- 基础认证联调验收：`done`
- 登录投票规则规格更新：`done`
- 三票限制规格更新：`done`
- 管理员后台规格更新：`done`
- 登录投票限制代码开发：`done`
- 管理员后台代码开发：`done`
- 新增能力联调验收：`done`
- 密码可见切换规格更新：`done`
- 密码可见切换代码开发：`done`
- 密码可见切换验证：`done`
- 修改密码规格更新：`done`
- 修改密码代码开发：`done`
- 修改密码验证：`done`
- 新一轮投票规格更新：`done`
- 新一轮投票代码开发：`done`
- 新一轮投票验证：`done`
- 完整图书信息规格更新：`done`
- 完整图书信息代码开发：`todo`
- 完整图书信息验证：`todo`

## 6. 验证记录

- 语法检查：已执行 `npm run check`，`server.js` 和 `public/app.js` 通过 Node.js 语法检查。
- 页面验证：已访问 `GET /`，服务返回前端页面 HTML。
- 列表接口验证：已访问 `GET /api/books`，返回 6 本图书和票数字段。
- 投票接口验证：已调用 `POST /api/votes`，传入有效 `bookId` 后返回最新票数。
- 错误接口验证：已调用 `POST /api/votes`，传入不存在的 `bookId` 后返回 `BOOK_NOT_FOUND`。
- 文件持久化验证：投票后已确认 `vote.json` 能被正确写入；验证后已恢复初始票数。
- 认证注册验证：已调用 `POST /api/auth/register`，成功创建测试用户。
- 认证重复用户名验证：重复调用注册接口返回 `USERNAME_EXISTS`。
- 认证登录失败验证：错误密码登录返回 `INVALID_CREDENTIALS`。
- 认证登录成功验证：正确密码登录返回用户信息并设置 `book_vote_session` Cookie。
- 当前用户验证：携带 Cookie 调用 `GET /api/auth/me` 返回 `authenticated: true` 和用户信息。
- 退出登录验证：调用 `POST /api/auth/logout` 清除 Cookie，再调用 `GET /api/auth/me` 返回未登录。
- 密码存储验证：确认 `user.json` 中不包含测试明文密码。
- 数据清理：认证验证后已清理测试用户，`user.json` 保留现有用户数据。
- 登录投票限制验证：未登录调用 `POST /api/votes` 返回 `UNAUTHORIZED`。
- 三票限制验证：测试用户成功投 3 票，第 4 票返回 `VOTE_LIMIT_REACHED`。
- 投票状态验证：投票接口返回 `userVoteCount` 和 `remainingVotes`。
- 管理员初始化验证：已通过 `ADMIN_USERNAME=admin_test ADMIN_PASSWORD=AdminPass123 npm run create-admin` 创建测试管理员。
- 后台权限验证：普通用户访问 `GET /api/admin/books` 返回 `FORBIDDEN`。
- 后台新增验证：管理员调用 `POST /api/admin/books` 成功新增测试图书并初始化票数。
- 后台改名验证：管理员调用 `PATCH /api/admin/books/:bookId` 成功修改测试图书书名。
- 后台删除验证：管理员调用 `DELETE /api/admin/books/:bookId` 成功删除测试图书并清理相关数据。
- 页面快检：已访问 `GET /`，确认后台管理入口和后台面板 HTML 已渲染。
- 数据清理：验证后已清理测试管理员、测试普通用户、测试投票和测试图书；保留原始票数。
- 密码可见切换验证：登录密码、注册密码、确认密码均已渲染显示/隐藏按钮，`npm run check` 通过。
- 修改密码验证：已验证错误当前密码返回 `INVALID_CURRENT_PASSWORD`，正确当前密码可修改成功，新密码可重新登录。
- 新一轮投票权限验证：未登录调用 `POST /api/admin/vote-rounds` 返回 `UNAUTHORIZED`，普通用户返回 `FORBIDDEN`。
- 新一轮投票执行验证：管理员调用 `POST /api/admin/vote-rounds` 成功返回 `roundStarted: true`，图书票数归零。
- 页面快检：已访问 `GET /`，确认“修改密码”表单和“发起新一轮投票”按钮已渲染。
- 数据恢复：验证后已恢复验证前的 `book.json`、`vote.json`、`user.json`，并重启服务清理临时 Session。
- 文档验证：本次仅更新 `spec.md` 和 `task.md`，未修改业务代码；后台维护完整图书信息尚未执行代码验证。

## 7. 后续同步进度更新规则

后续开发过程中，应持续更新本文件，确保任务状态与实际进度一致。

### 7.1 状态更新规则

- 开始某项任务时，将状态从 `todo` 改为 `in-progress`。
- 完成某项任务并通过验收后，将状态改为 `done`。
- 如果任务实现完成但未验证，仍保持 `in-progress`。
- 如果发现任务拆分不合理，可以新增、拆分或合并任务，但需保持验收标准清晰。

### 7.2 进度摘要更新规则

每次完成一个阶段后，应更新“当前项目进度摘要”：

- 说明已完成的阶段。
- 说明正在进行的阶段。
- 说明下一步计划。
- 记录发现的风险、阻塞或范围变化。

### 7.3 验收记录规则

每次完成关键能力后，建议记录验证方式：

- 接口验证：记录请求路径、请求方法和期望响应。
- 页面验证：记录用户操作路径和预期页面反馈。
- 文件验证：记录 `book.json`、`vote.json`、`user.json` 是否被正确读取或写入。

### 7.4 范围控制规则

后续开发期间应优先完成 `spec.md` 中定义的核心能力。以下需求暂不进入当前开发范围：

- 数据库迁移。
- 找回密码和第三方登录。
- 复杂 UI 动效。

如需加入新功能，应先更新 `spec.md`，再同步更新 `task.md`。
